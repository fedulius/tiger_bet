Работай только в /home/fedulov/tiger_bet. Это фаза 2, шаг D. Используй текущие незакоммиченные изменения как базу. Не трогай никакие файлы, кроме lib/forecastAnalyzer.js и tests/webapp/recommendation-service-live.test.js. НЕ изменяй webapp/services/recommendationService.js, другие тесты, React/UI, feed, config, CLAUDE.md и любые другие файлы. Не делай commit/push. Не делай широкий рефакторинг.

Текущий статус:
- extractRecommendationZones(...) уже есть;
- analyzeForecastFromHtml(...) уже работает по zones;
- parseCanonicalMarket(...) уже покрывает 1x2 / double chance / totals / btts / handicap / individual totals;
- canonical normalization уже частично встроена в percent/odds signals.

Но по первоначальному плану фазы 2 не хватает нескольких узких вещей. Твоя задача — добить только их, без выхода за scope.

Что нужно сделать в этом шаге D:
1) Вокруг существующего parseCanonicalMarket(...) добавь недостающий promised canonical surface:
   - helper по смыслу normalizeMarketText(...)
   - helper по смыслу buildMarketKey(...)
   Названия можешь слегка адаптировать, но смысл должен быть читаемым и явным в коде.
2) Не ломая уже существующую поддержку, добавь однозначные aliases:
   - «победа хозяев» -> П1
   - «победа гостей» -> П2
   - только если формулировка однозначна: «хозяева не проиграют» / «гости не проиграют» -> соответствующий double chance
3) Добавь явную защиту от malformed market fragments, похожих на line мусор, например:
   - «60 ФОРА 2 (»
   - «44 ФОРА 1 (»
   - обрывки с незакрытыми скобками
   - строки, которые очевидно похожи на битую line-table строку, а не на human-readable forecast
   На этом шаге достаточно, чтобы такие строки не проходили canonical normalization и не получали marketKey.
4) Не меняй service layer и не строй новый candidate pipeline. Только canonical helper layer и tests.
5) Добавь 4–8 точечных тестов только в tests/webapp/recommendation-service-live.test.js:
   - aliases для победы хозяев/гостей
   - aliases для однозначного «не проиграет»
   - buildMarketKey/normalizeMarketText для пары supported markets
   - malformed fragments не проходят как valid canonical market surface

Acceptance criteria:
1) В lib/forecastAnalyzer.js есть явные helper-ы по смыслу normalizeMarketText(...) и buildMarketKey(...);
2) parseCanonicalMarket(...) + эти helper-ы покрывают aliases победы хозяев/гостей и однозначное «не проиграет»;
3) malformed fragments не canonicalize-ятся;
4) изменены только 2 разрешённых файла максимум;
5) после изменений запускается только:
node --test tests/webapp/recommendation-service-live.test.js

В финале выведи только:
1) changed files,
2) выполнен ли шаг D (да/нет),
3) кратко что изменено,
4) результат команды с exit code.

Если видишь, что какой-то пункт нельзя сделать без расползания scope — не расширяй scope, а прямо напиши это.